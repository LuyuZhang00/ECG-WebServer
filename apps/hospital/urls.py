from django.urls import path
from . import views
from rest_framework.routers import DefaultRouter, SimpleRouter

# router = DefaultRouter()
# router.register(r'hospital', views.HospitalManageViewSet, basename='hospital')

urlpatterns = [

]